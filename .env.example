# Environment Variable Template for async_delay.h
# Note: async_delay.h is a header-only embedded C library for 8-bit AVR microcontrollers.
# No runtime API keys or environment variables are required.

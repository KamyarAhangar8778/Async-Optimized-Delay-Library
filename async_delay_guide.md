# 📖 راهنمای استفاده از کتابخانه async_delay

کتابخانه‌ی **async_delay** یک تاخیرِ **غیر-مسدودکننده (non-blocking)** برای میکروکنترلرهای AVR است.

## 🤔 اصلاً این یعنی چه؟

وقتی می‌نویسی `delay_ms(500)`، میکرو **۵۰۰ میلی‌ثانیه کاملاً قفل** می‌شود و هیچ کاری نمی‌کند (نه LED، نه LCD، نه سنسور، هیچ‌چیز).

با `async_delay`، به میکرو می‌گویی: «بعد از ۵۰۰ms فلان کار را بکن» و **همان لحظه** برمی‌گردی به حلقه‌ی اصلی. میکرو در آن ۵۰۰ms آزاد است و بقیه‌ی کارها را انجام می‌دهد. وقتی ۵۰۰ms تمام شد، کارت انجام می‌شود.

---

## 🧱 ساختار کلی (۵ قدم)

```
1. فلگ‌های پیکربندی را تعریف کن
2. یک تایمر سخت‌افزاری را تنظیم کن که هر 1ms قطع (interrupt) بزند
3. داخل ISR تایمر، تابع async_delay_tick() را صدا بزن
4. در main() یک‌بار async_delay_init() را صدا بزن
5. از توابع async_delay_* در برنامه‌ات استفاده کن
```

---

## 📌 قدم ۱: فلگ‌های پیکربندی

این‌ها را **قبل از** `#include` تعریف کن:

```c
#define ASYNC_DELAY_TIMER_BITS  16    // عرض شمارنده: 8 یا 16 یا 32 (پیش‌فرض 16)
#define ASYNC_DELAY_MAX_SLOTS   4     // حداکثر چند تاخیر هم‌زمان (پیش‌فرض 4)
#define ASYNC_DELAY_TICK_HZ     1000  // چند تیک در ثانیه؟ 1000 یعنی هر تیک = 1ms

#include <async_delay.h>
```

**توضیح هر فلگ:**

| فلگ | معنی | مثال |
|-----|------|------|
| `ASYNC_DELAY_TICK_HZ` | نرخ تیک. **اجباری است.** | `1000` یعنی ۱ تیک = ۱ میلی‌ثانیه |
| `ASYNC_DELAY_TIMER_BITS` | عرض شمارنده‌ی داخلی. `16` برای اکثر کارها کافی است | `16` |
| `ASYNC_DELAY_MAX_SLOTS` | چند تاخیر می‌توانی هم‌زمان فعال داشته باشی | `4` |

> ⚠️ اگر `ASYNC_DELAY_TICK_HZ` را تعریف نکنی، کامپایلر همان اول **خطا** می‌دهد. این عمدی است تا یادت نرود.

---

## 📌 قدم ۲ و ۳: تنظیم تایمر (مهم‌ترین قسمت)

باید یک تایمر سخت‌افزاری را طوری تنظیم کنی که هر `1/TICK_HZ` ثانیه قطع بزند، و داخل قطع، `async_delay_tick()` را صدا بزنی.

**چرا؟** چون `async_delay_tick()` همان «قلب ساعت» کتابخانه است. هر بار که صدا زده شود، یک تیک جلو می‌رود و بررسی می‌کند کدام تاخیرها تمام شده‌اند.

### مثال کامل برای ATmega8 با ساعت 8MHz (تایمر 2):

```c
// مقدار OCR2 برای 1ms با فرکانس‌های مختلف:
//   8MHz  (پری‌اسکالر 64) → 8MHz/64 = 125kHz → 1ms = 125 تیک → OCR2 = 124
//  16MHz  (پری‌اسکالر 64) → 16MHz/64 = 250kHz → 1ms = 250 تیک → OCR2 = 249

#define OCR2_1MS  124    // اگر کلاک 8MHz است

// ISR تایمر 2 — هر 1ms یک‌بار اجرا می‌شود
interrupt [TIM2_COMP] void timer2_comp_isr(void)
{
    async_delay_tick();   // قلب ساعت کتابخانه
}

// تابع راه‌اندازی — در main() قبل از هر چیز صدا بزن
void timer2_init(void)
{
    ASSR = 0x00;                                        // تایمر از کلاک سیستم
    TCCR2 = (1 << WGM21)                                // حالت CTC
          | (0 << COM21) | (0 << COM20)                 // خروجی OC2 غیرفعال
          | (1 << CS22) | (0 << CS21) | (0 << CS20);    // پری‌اسکالر /64
    TCNT2 = 0x00;
    OCR2  = OCR2_1MS;                                   // برای تیک 1ms
    TIMSK |= (1 << OCIE2);                              // فعال‌کردن قطع تایمر 2
}
```

### 🎯 برای فرکانس و چیپ خودت چطور محاسبه کنی؟

```
فرکانس تایمر = Clock میکرو / پری‌اسکالر
مقدار OCR   = (فرکانس تایمر / 1000) - 1      ← برای تیک 1ms
```

مثال‌ها:

| چیپ | کلاک | پری‌اسکالر | فرکانس تایمر | OCR |
|-----|------|-----------|--------------|-----|
| ATmega8 | 8MHz | /64 | 125 kHz | 124 |
| ATmega8 | 16MHz | /64 | 250 kHz | 249 |
| ATmega8 | 1MHz | /8 | 125 kHz | 124 |
| ATmega32 | 8MHz | /64 | 125 kHz | 124 |
| ATmega32 | 16MHz | /64 | 250 kHz | 249 |

### ⚠️ تله‌ی رایج: عوض کردن فرکانس ولی فراموش‌کردن Compare

هر بار که **فرکانس تایمر (Clock)** را عوض می‌کنی، باید **مقدار OCR/Compare را دوباره حساب کنی** — وگرنه دوره اشتباه می‌شود.

```
Period (ms) = (Compare + 1) / TimerClock
```

مثال واقعی که اتفاق افتاد: با `Compare = 249`:
- Clock تایمر = **250kHz** → `250/250000` = **1ms** ✅
- Clock تایمر = **125kHz** → `250/125000` = **2ms** ❌ (باز هم همان Compare را نگه داشتی!)

پس این دو به هم وابسته‌اند:

| Clock تایمر | Compare (برای 1ms) |
|-------------|--------------------|
| 250.000 kHz | 249 |
| 125.000 kHz | 124 |

> 💡 در ویزارد CodeVisionAVR هم اگر فیلد زمان (مثلاً `1.000 ms`) را پر کنی، خودش Compare را بر اساس Clock انتخابی حساب می‌کند — ولی اگر خودت عدد Compare را تایپ می‌کنی، حتماً دوباره محاسبه کن.

### 🚨 نکته‌ی مهم برای فیلد Compare در ویزارد: دسیمال تا ۹۹، بقیه هگز

فیلد **Compare** در CodeWizardAVR:
- اگر به صورت **دسیمال** بنویسی، فقط **تا ۹۹** قبول می‌کند (۲ رقم).
- برای مقادیر بزرگ‌تر باید **هگزادسیمال** بنویسی (پیشوند `0x` یا `0X`، مثلاً `0x7C`).

مثال: `124` اگر دسیمال تایپ شود، در ویزارد به `80` تبدیل می‌شود (رقم‌ها جدا خوانده می‌شوند) — ولی `0x7C` (هگز) مقدار درست 124 را می‌دهد.

**تبدیل برای تیک 1ms با فرکانس‌های رایج:**

| Clock تایمر | Compare (دسیمال) | در ویزارد بنویس |
|-------------|------------------|-----------------|
| 125.000 kHz | 124 | **`0x7C`** |
| 250.000 kHz | 249 | **`0xF9`** |

**تبدیل دسیمال → هگز (بدون ماشین‌حساب):**
`124 / 16 = 7` باقی‌مانده `12` → رقم‌های هگز: `7` و `C` → `0x7C`
(۱۲→C، ۱۳→D، ۱۴→E، ۱۵→F)

| دسیمال | هگز |
|--------|-----|
| 61 | 3D |
| 124 | 7C |
| 249 | F9 |

> ⚠️ همیشه یادت باشد: **فیلد دسیمال فقط ۲ رقم است.** هر عددی که از ۹۹ بیشتر است را به هگز بنویس.

### 📋 جدول کامل تیک 1ms برای همهٔ فرکانس‌ها (ATmega8/16/32)

فرمول: `Compare = (فرکانس میکرو / Prescaler / 1000) - 1`

| فرکانس میکرو | Prescaler | Clock تایمر | Compare (دسیمال) | در ویزارد (هگز) |
|--------------|-----------|-------------|------------------|-----------------|
| 1 MHz | /8 | 125.000 kHz | 124 | **0x7C** |
| 2 MHz | /8 | 250.000 kHz | 249 | **0xF9** |
| 4 MHz | /32 | 125.000 kHz | 124 | **0x7C** |
| 8 MHz | /64 | 125.000 kHz | 124 | **0x7C** |
| 16 MHz | /64 | 250.000 kHz | 249 | **0xF9** |

**تفاوت چیپ‌ها در تایمرها:**

| چیپ | Timer0 | Timer2 | Timer1 |
|-----|--------|--------|--------|
| ATmega8 | ❌ CTC ندارد | ✅ CTC دارد | ✅ CTC دارد (16-bit) |
| ATmega16/32 | ✅ CTC دارد | ✅ CTC دارد | ✅ CTC دارد (16-bit) |

> روی ATmega16/32 سه تایمر CTC داری: Timer0 و Timer2 (8-bit) + Timer1 (16-bit). مثلاً تیک 1ms را روی Timer0 بگذار و Timer2 را برای PWM آزاد بگذار. مقادیر جدول بالا برای Timer0 هم یکسان است.

---

## 🎯 تایمر سخت‌افزاری باید چه ویژگی‌هایی داشته باشد؟

کتابخانه به **تایمر به‌عنوان «ساعت»** نیاز دارد. تایمر لازم نیست کار خاصی انجام دهد — فقط باید **هر `1/TICK_HZ` ثانیه یک قطع (interrupt)** بزند. مشخصات لازم:

| ویژگی | چرا لازم است؟ |
|-------|---------------|
| **قطع دوره‌ای (Periodic Interrupt)** | باید با نرخ ثابت `TICK_HZ` قطع بزند. این قطع است که `async_delay_tick()` را صدا می‌زند |
| **حالت مقایسه (CTC / Compare Match)** | با رجیستر `OCR` می‌توانی دوره را **دقیق** تنظیم کنی (مثلاً دقیقاً 1ms) |
| **پری‌اسکالر (Prescaler)** | کلاک میکرو را پایین می‌آورد تا به فرکانس مناسب برای تیک برسی |
| **بیت فعال‌کردن قطع** | مثل `OCIE2` یا `TOIE0` — تا قطع تایمر واقعاً به CPU برسد |
| **ISR کوتاه** | خودِ ISR فقط باید `async_delay_tick()` را صدا بزند — نه کار دیگر |

> ✅ تایمر فقط باید قطع بزند؛ بقیه‌ی قابلیت‌هایش (PWM، خروجی OC) **آزاد** است و می‌توانی هم‌زمان استفاده‌شان کنی.
> ⚠️ فقط مواظب باش **دو چیز هم‌زمان** رجیسترهای یک تایمر را مقداردهی نکنند (مثلاً تو برای تیک و کتابخانه‌ی دیگه برای PWM).

### کدام تایمر را برای ATmega8 انتخاب کنم؟

| تایمر | عرض | حالت CTC | مناسب برای تیک؟ |
|-------|-----|----------|-----------------|
| **Timer2** | 8 بیت | دارد ✅ | ✅ بهترین برای تیک 1ms |
| **Timer1** | 16 بیت | دارد ✅ | ✅ دقت بالاتر، دوره‌ی طولانی‌تر |
| **Timer0** | 8 بیت | ندارد ❌ (فقط overflow) | ⚠️ کمتر مناسب (کنترل دقیق دوره سخت‌تر است) |

> 💡 اگر `TICK_HZ = 1000` (تیک 1ms) با Timer2 در 8MHz: `OCR2 = 124`. با Timer1 می‌توانی تیک را کوتاه‌تر هم بکنی (مثلاً 100µs) یا دوره‌های طولانی‌تر مدیریت کنی.

---

## 📌 قدم ۴: راه‌اندازی اولیه

در `main()`، **بعد از** راه‌اندازی تایمر و **قبل از** فعال‌کردن وقفه‌های سراسری:

```c
async_delay_init();   // همه‌ی اسلات‌ها را آزاد می‌کند و شمارنده را صفر می‌کند
#asm("sei")           // فعال‌کردن وقفه‌های سراسری (بعد از همه‌ی init ها)
```

---

## 📌 قدم ۵: استفاده از توابع (۴ تابع)

### ۱) `async_delay_start(duration, callback)` — تاخیر یک‌باره

بعد از `duration` تیک یک‌بار اجرا می‌شود، بعد تمام می‌شود.

```c
// بعد از 500ms یک‌بار led_off را صدا بزن
async_delay_start(500, led_off);
```

### ۲) `async_delay_start_periodic(duration, callback)` — تاخیر تکرارشونده

هر `duration` تیک اجرا می‌شود و **خودش دوباره شروع می‌شود** تا وقتی `cancel` کنی. برای چشمک زدن LED عالی است.

```c
// هر 500ms یک‌بار led_toggle را صدا بزن (تا ابد، مگر لغو شود)
async_delay_start_periodic(500, led_toggle);
```

### ۳) `async_delay_elapsed(slot_id)` — بررسی دستی (پرس‌وجو)

به جای callback، خودت در حلقه چک می‌کنی «تمام شد یا نه؟». `1` برمی‌گرداند اگر تمام شده باشد (و اسلات را آزاد می‌کند).

```c
unsigned char id = async_delay_start(1000, (void *)0);   // بدون callback
while (1)
{
    if (async_delay_elapsed(id))   // آیا 1000 تیک گذشت؟
    {
        // یک‌بار کار را انجام بده
        do_something();
    }
    // بقیه‌ی کارهای حلقه...
}
```

### ۴) `async_delay_cancel(slot_id)` — لغو تاخیر

یک تاخیر فعال را لغو می‌کند.

```c
unsigned char id = async_delay_start(500, do_something);
// ...بعداً پشیمان شدی...
async_delay_cancel(id);   // دیگر اجرا نمی‌شود
```

---

## 🧠 مفاهیم مهم

### اسلات (Slot) چیست؟
اسلات یک «جا» در کتابخانه برای یک تاخیر است. با `MAX_SLOTS` تعدادش را تعیین می‌کنی. هر تاخیری که شروع کنی یک اسلات را اشغال می‌کند. وقتی تمام شود، اسلات آزاد می‌شود.

- `start` مقدار **شماره اسلات** را برمی‌گرداند (0 تا MAX_SLOTS-1)
- اگر اسلات خالی نباشد، **`0xFF`** برمی‌گرداند

### Callback چیست؟
تابعی است که کتابخانه وقتی تاخیر تمام شد، خودش صدا می‌زند. **فقط باید کار کوتاه انجام دهد** (مثلاً یک فلگ را عوض کند) چون داخل ISR اجرا می‌شود.

```c
void led_toggle(unsigned char slot_id)   // این تابع callback است
{
    led_state ^= 1;    // فقط یک کار کوتاه!
}
```

### چرا callback را کوتاه نگه دارم؟
چون وقتی تاخیر تمام می‌شود، میکرو وسط یک قطع (interrupt) است. اگر کار طولانی بکنی (مثلاً `delay_ms` یا چاپ LCD)، بقیه‌ی قطع‌ها و کارهای مهم به‌هم می‌ریزند. **فقط یک فلگ یا یک پین را عوض کن.**

---

## 🚫 اشتباهات رایج

| اشتباه | نتیجه | راه درست |
|--------|-------|----------|
| فراموش‌کردن `ASYNC_DELAY_TICK_HZ` | خطای کامپایلر | قبل از include تعریف کن |
| صدا نزدن `async_delay_tick()` در ISR | هیچ تاخیری کار نمی‌کند | داخل ISR تایمر صدا بزن |
| صدا زدن `async_delay_init()` بعد از `sei` | شمارنده‌ها از اول صفر می‌شوند | قبل از `sei` صدا بزن |
| فراموش‌کردن `sei` | هیچ‌چیز کار نمی‌کند | بعد از همه init ها `#asm("sei")` |
| دوری زدن `async_delay_start` (نه periodic) برای چشمک | LED یک‌بار روشن می‌شود و می‌ماند | از `async_delay_start_periodic` استفاده کن |
| کار طولانی داخل callback | به‌هم‌ریختن زمان‌بندی | فقط فلگ/پین را عوض کن |

---

## 🔢 دقت و محدودیت

- **خطای کتابخانه**: حداکثر ±1 تیک (یعنی ±1ms با تیک 1ms)
- **دقت واقعی** را **کلاک میکرو** تعیین می‌کند:
  - کریستال خارجی: ~±0.005% (بسیار دقیق)
  - اسیلاتور داخلی RC: ~±1% تا ±3%
- **حداکثر تاخیر مطمئن** با تیک 16 بیتی: ~32767 تیک (≈32 ثانیه با تیک 1ms)
  - بیشتر از این غیرقابل اعتماد است (ابهام سرریز)

---

## 📝 مثال حداقلی و کامل

```c
#include <mega8.h>
#include <async_delay.h>

#define OCR2_1MS  124    // برای 8MHz

unsigned char led_state = 0;

// callback: فقط یک فلگ را عوض کن (کوتاه!)
void blink_cb(unsigned char slot_id)
{
    led_state ^= 1;
}

// ISR تایمر 2 — قلب ساعت کتابخانه
interrupt [TIM2_COMP] void timer2_comp_isr(void)
{
    async_delay_tick();
}

void main(void)
{
    DDRB = 0x01;                    // PB0 خروجی (LED)

    // راه‌اندازی تایمر 2 برای تیک 1ms
    // ✅ این را می‌توانی با ویزارد CodeVisionAVR هم بسازی (به زیر مراجعه کن)
    ASSR = 0x00;
    TCCR2 = (1 << WGM21) | (1 << CS22);   // CTC + پری‌اسکالر /64
    TCNT2 = 0x00;
    OCR2  = OCR2_1MS;
    TIMSK |= (1 << OCIE2);

    async_delay_init();             // راه‌اندازی کتابخانه

    // هر 500ms یک‌بار blink_cb اجرا می‌شود
    async_delay_start_periodic(500, blink_cb);

    #asm("sei")                     // فعال‌کردن وقفه‌ها

    while (1)
    {
        PORTB.0 = led_state;        // اعمال حالت LED

        // >>> اینجا حلقه آزاد است — هر کاری می‌خواهی بکن <<<
    }
}
```

---

## ⚙️ تایمر را ویزارد CodeVisionAVR می‌سازد یا خودم باید بنویسم؟

**هر دو درست است — ولی توصیه‌ی من: ویزارد.**

### راه ۱: ویزارد CodeVisionAVR (پیشنهادی ✅)
ویزارد برایت رجیسترهای تایمر و ISR آن را **خودکار** می‌سازد. فقط:
1. در CodeWizardAVR، سربرگ **Timer2** را باز کن
2. Clock Source: `SysClk/64`
3. **Compare Match**: تیک بزن (اجازه‌ی وقفه)
4. مقدار **Compare Value**: `124` (برای 8MHz و تیک 1ms)
5. ویزارد می‌سازد:
   - مقداردهی `TCCR2`، `TCNT2`، `OCR2`، `TIMSK` → اینها دیگر **نمی‌نویسی**، ویزارد داده
   - اسکلت ISR → **تنها کاری که می‌کنی** این است که داخلش `async_delay_tick();` را اضافه کنی

> 💡 بعد از تولید کد، فقط عبارت `async_delay_tick();` را داخل ISRِ ساخته‌شده‌ی ویزارد قرار بده.

### راه ۲: دستی (همین مثال)
می‌توانی رجیسترها را خودت بنویسی (مثل کد بالا). این زمانی خوب است که:
- ویزارد در دسترس نباشد
- بخواهی دقیقاً بدانی چه اتفاقی می‌افتد
- مقدارها را عوض کنی (مثلاً تیک 500µs)

### چرا پیشنهاد ویزارد؟
- خطای انسانی کمتر
- مقداردهی کامل (شامل `ASSR` و حالت‌های مربوطه)
- همه‌چیز با کد `mega8.h` سازگار است

> ⚠️ هر دو راه، فقط بخش تایمر را تنظیم می‌کنند. **کتابخانه async_delay همیشه دستی اضافه می‌شود** (فلگ‌ها + include + `async_delay_init()` + فراخوانی توابع) — ویزارد درباره‌ی کتابخانه‌ی ما هیچ نمی‌داند.

---

## 🏁 خلاصه‌ی خیلی کوتاه

```
1. فلگ‌ها را تعریف کن (قبل از include)
2. تایمر را تنظیم کن که هر 1ms قطع بزند
3. داخل ISR تایمر → async_delay_tick()
4. در main → async_delay_init()  و بعد  #asm("sei")
5. async_delay_start_periodic(500, my_cb)  → هر 500ms کار انجام می‌شود
6. در حلقه‌ی اصلی فقط بقیه‌ی کارها را بکن، دیگر قفل نمی‌شوی!
```

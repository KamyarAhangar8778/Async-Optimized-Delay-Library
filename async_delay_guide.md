# 📖 راهنمای جامع استفاده از کتابخانه async_delay (نسخه ۱۲)

کتابخانه‌ی **async_delay** یک راهکار سبک، هدر-تنها (Header-only) و **غیر-مسدودکننده (Non-blocking)** برای مدیریت زمان و تاخیرها در میکروکنترلرهای AVR (**ATmega8 / ATmega16 / ATmega32**) با کامپایلر **CodeVisionAVR** است.

---

## 🎯 مفهوم تاخیر غیر-مسدودکننده چیست؟

در روش سنتی با `delay_ms(500)`، پردازنده ۵۰۰ میلی‌ثانیه کاملاً قفل می‌شود و هیچ کار دیگری (بررسی دکمه، خواندن سنسور، رفرش نمایشگر) انجام نمی‌دهد.

با **async_delay**، به پردازنده می‌گویید: «کار X را بعد از ۵۰۰ میلی‌ثانیه انجام بده» و همان لحظه کنترل به حلقه‌ی اصلی برنامه برمی‌گردد. شمارش زمان در پس‌زمینه توسط وقفه (ISR) تایمر سخت‌افزاری انجام می‌شود.

---

## 🧱 مراحل راه‌اندازی (۷ مرحله‌ی اساسی)

```text
۱. افزودن هدر میکروکنترلر (مثلاً mega8.h) — حتماً قبل از هدر کتابخانه
۲. تعریف فلگ‌های پیکربندی (حداقل ASYNC_DELAY_TICK_HZ)
۳. اینکلود کردن هدر async_delay.h (فقط در یک فایل c.)
۴. تنظیم تایمر سخت‌افزاری در حالت CTC برای ایجاد وقفه در هر 1/TICK_HZ ثانیه
۵. صدا زدن async_delay_tick() درون ISR تایمر
۶. صدا زدن async_delay_init() در main() قبل از هر تابعی و قبل از فعال‌سازی وقفه
۷. فعال‌سازی وقفه‌ها با دستور asm("sei")#
```

### ترتیب بسیار مهم اینکلودها:
```c
#include <mega8.h>             // ۱. اول هدر میکرو (برای تعریف رجیستر SREG الزامی است)

#define ASYNC_DELAY_TICK_HZ 1000  // ۲. تنظیمات قبل از اینکلود کتابخانه
#define ASYNC_DELAY_TIMER_BITS 16
#define ASYNC_DELAY_MAX_SLOTS 4

#include <async_delay.h>      // ۳. سپس خود کتابخانه
```

> ⚠️ **نکته‌ی مهم:** اگر هدر میکرو قبل از `async_delay.h` قرار نگیرد، با خطای `undefined symbol 'SREG'` مواجه می‌شوید.

---

## ⚙️ پارامترهای پیکربندی و فلگ‌های بهینه‌سازی

این ماکروها باید **قبل از** `#include <async_delay.h>` تعریف شوند:

### تنظیمات اصلی:
| نام ماکرو | پیش‌فرض | وضعیت | کاربرد |
|---|---|---|---|
| `ASYNC_DELAY_TICK_HZ` | — | **اجباری** | نرخ تیک در ثانیه (مثلاً `1000` یعنی هر تیک = ۱ میلی‌ثانیه). |
| `ASYNC_DELAY_TIMER_BITS` | `16` | اختیاری | عرض شمارنده (`8`، `16` یا `32` بیت). حداکثر مدت تاخیر مطمئن را تعیین می‌کند. |
| `ASYNC_DELAY_MAX_SLOTS` | `4` | اختیاری | حداکثر تعداد تاخیرهای هم‌زمان (پیش‌فرض ۴، پشتیبانی تا ۱۶ اسلات هم‌زمان با بیت‌ماسک). |
| `ASYNC_DELAY_DEFERRED_CALLBACKS` | `0` | اختیاری | حالت Callbacks معوق در حلقه‌ی اصلی (`1` برای کارهای سنگین نظیر LCD یا UART). |

### فلگ‌های درستی عملکرد و بهینه‌سازی (پیش‌فرض فعال):
| نام فلگ | پیش‌فرض | دسته‌بندی | توضیح فنی |
|---|---|---|---|
| `ASYNC_DELAY_CALLBACK_RESCHEDULE` | `1` | صحت عملکرد | آزادسازی اسلات قبل از اجرای Callback جهت امکان زمان‌بندی مجدد خودکار (Self-reschedule). |
| `ASYNC_DELAY_OPT_FAST_CANCEL` | `1` | کارایی | لغو سریع در زمان $O(1)$؛ اگر اسلات لغوشده زودترین اسلات نباشد، محاسبه مجدد کل اسلات‌ها رد می‌شود (~۲۲ سیکل). |
| `ASYNC_DELAY_FEATURE_RESTART` | `1` | قابلیت جدید | فعال‌سازی تابع `async_delay_restart` برای تغییر زمان اسلات درجا بدون بازتخصیص یا تغییر شناسه. |
| `ASYNC_DELAY_FIX_USED_MASK` | `1` | صحت عملکرد | تفکیک اسلات‌های تخصیص‌یافته از اسلات‌های در حال اجرا جهت جلوگیری از سرقت اسلات‌های منقضی‌شده. |
| `ASYNC_DELAY_FIX_ATOMIC_MASK` | `1` | هم‌زمانی | حفاظت اتمیک با حفظ مقدار `SREG` در زمان تغییر ماسک‌ها در کانتکست اصلی. |
| `ASYNC_DELAY_OPT_BITMASK` | `1` | کارایی | پیمایش فقط اسلات‌های فعال با بیت‌ماسک (پشتیبانی تا ۱۶ اسلات با نوع `async_mask_t`). |
| `ASYNC_DELAY_OPT_LUT_MASK` | `1` | کارایی فوق‌العاده | جدول Lookup Table بیت‌ماسک (`_async_slot_bit`) جهت حذف حلقه‌های سنگین شیفت بیت (`__LSLW12`) در AVR و اجرای O(1) در ۲ سیکل. |
| `ASYNC_DELAY_OPT_LUT_ALLOC` | `1` | کارایی فوق‌العاده | جدول نیبل تخصیص اسلات (`_async_first_free_nibble`) جهت جستجوی فوری و بدون حلقه اولین اسلات خالی در O(1). |
| `ASYNC_DELAY_OPT_LUT_POPCOUNT` | `1` | کارایی فوق‌العاده | جدول نیبل Popcount (`_async_popcount_nibble`) جهت شمارش فوری تعداد اسلات‌های فعال بدون حلقه در زمان O(1). |
| `ASYNC_DELAY_DISABLE_CALLBACKS` | `0` | کاهش مصرف حافظه | حذف اشاره‌گرهای توابع Callback در پروژه‌های صرفاً Polling (صرفه‌جویی ۲ بایت RAM به ازای هر اسلات، ۲۳.۵٪ کاهش کل RAM). |
| `ASYNC_DELAY_DISABLE_PERIODIC` | `0` | کاهش مصرف حافظه | حذف فیلد duration و کدهای تکرار در پروژه‌های One-Shot (صرفه‌جویی ۲ بایت RAM به ازای هر اسلات). |
| `ASYNC_DELAY_OPT_NEXT_TARGET` | `1` | کارایی | کش کردن نزدیک‌ترین زمان انقضا برای خروج $O(1)$ از ISR بدون بررسی تک‌تک اسلات‌ها. |
| `ASYNC_DELAY_OPT_SPLIT_TICK` | `1` | کارایی | حذف متغیرهای محلی از تابع تیک اصلی برای جلوگیری از سربار Register Spill در CodeVisionAVR. |
| `ASYNC_DELAY_OPT_UNROLL_TICK` | `1` | کارایی | باز کردن حلقه‌ی اسلات‌ها با ایندکس‌های ثابت زمان کامپایل (تا ۱۶ اسلات، حذف محاسبات ضرب و شیفت). |
| `ASYNC_DELAY_OPT_MERGED_FLAGS` | `1` | مصرف حافظه | ادغام وضعیت و بیت تکرار در یک بایت منفرد جهت صرفه‌جویی در SRAM. |
| `ASYNC_DELAY_OPT_SPLIT_ARRAYS` | `0` | مصرف حافظه | استفاده از آرایه‌های موازی به جای آرایه‌ی Struct (اختیاری). |

### حداکثر زمان قابل تنظیم بر اساس `TIMER_BITS` (با تیک 1ms):
- شمارنده **۸ بیتی**: تا ۱۲۷ تیک (~۱۲۷ میلی‌ثانیه)
- شمارنده **۱۶ بیتی**: تا ۳۲,۷۶۷ تیک (~۳۲.۷ ثانیه)
- شمارنده **۳۲ بیتی**: تا بیش از ۲ میلیارد تیک (~۲۴.۸ روز)

> ⚠️ **فرمول تبدیل زمان:** ورودی توابع برحسب «تیک» است، نه میلی‌ثانیه:
> $$\text{Ticks} = \frac{\text{ms} \times \text{ASYNC\_DELAY\_TICK\_HZ}}{1000}$$

### آمار دقیق بهبودها و مصرف منابع در نسخه ۹ (اندازه‌گیری روی ATmega8 @ 8 MHz):
- **سرعت `async_delay_restart`:** افزایش سرعت به میزان **~۳۳٪** (کاهش از ۴۸ سیکل به ۳۲ سیکل با حذف شیفت‌های تکراری و بهره‌گیری از LUT).
- **سرعت `async_delay_elapsed`:** افزایش سرعت به میزان **~۵۰٪** (کاهش از ۲۸ سیکل به ۱۴ سیکل برای اسلات فعال).
- **سرعت `async_delay_poll`:** تا **~۴۰٪ سریع‌تر** با پیاده‌سازی خروج زودهنگام (Early Exit) روی ماسک معوق و عدم پیمایش بقیه اسلات‌ها.
- **بهینه‌سازی حافظه SRAM در حالت Polling-Only (`DISABLE_CALLBACKS=1`):** کاهش مصرف RAM به میزان **۲۳.۵٪** (کاهش از ۳۴ بایت به ۲۶ بایت در ۴ اسلات؛ در ۱۶ اسلات تا ۳۲ بایت صرفه‌جویی می‌شود).
- **بهینه‌سازی ترکیبی با `DISABLE_PERIODIC=1`:** کاهش حجم ساختار اسلات از ۷ بایت به ۳ بایت (**۵۷٪ صرفه‌جویی در رم هر اسلات**).
- **حافظه Flash:** حدود ۳۹۵ کلمه در حالت عادی، و با غیرفعال‌سازی Callback تا ۱۵٪ کاهش در حجم Flash.
- **سربار وقفه در حالت بیکار:** فقط **~۸۵ سیکل ساعت** (~۱۰.۶ میکروثانیه، حدود ۱٪ بار پردازنده در تیک ۱ کیلوهرتز).

---

## 🔌 تنظیم تایمر سخت‌افزاری (CTC Mode)

تایمر سخت‌افزاری باید دقیقاً با فرکانس `ASYNC_DELAY_TICK_HZ` وقفه بدهد. درون ISR تایمر **فقط و فقط** باید تابع `async_delay_tick()` فراخوانی شود.

### فرمول محاسبه رجیستر OCR:
$$\text{OCR} = \left( \frac{F_{\text{CPU}}}{\text{Prescaler} \times \text{ASYNC\_DELAY\_TICK\_HZ}} \right) - 1$$

### مقادیر آماده برای تیک ۱ میلی‌ثانیه‌ای (`TICK_HZ = 1000`):

| کلاک میکرو | تایمر انتخابی | پری‌اسکالر | فرکانس تایمر | مقدار OCR (دسیمال) | در CodeWizard یا هگز |
|---|---|---|---|---|---|
| **1 MHz** | Timer2 / Timer0* | `/8` | 125 kHz | **124** | `0x7C` |
| **8 MHz** | Timer2 | `/64` | 125 kHz | **124** | `0x7C` |
| **16 MHz** | Timer2 | `/64` | 250 kHz | **249** | `0xF9` |
| **8 MHz** | Timer1 (16-bit) | `/64` | 125 kHz | **124** | `0x007C` |
| **16 MHz** | Timer1 (16-bit) | `/64` | 250 kHz | **249** | `0x00F9` |

*\*در ATmega8 تایمر ۰ حالت CTC ندارد؛ از تایمر ۲ یا ۱ استفاده کنید. در ATmega16/32 تایمر ۰ از CTC پشتیبانی می‌کند.*

> 🚨 **نکته‌ی حیاتی در CodeWizardAVR:**
> کادر Compare در ویزارد فقط ۲ رقم دسیمال می‌پذیرد (نوشتن ۱۲۴ باعث ذخیره اشتباه ۸۰ می‌شود). **همیشه اعداد بالای ۹۹ را به هگز بنویسید:**
> - برای ۱۲۴ بنویسید: `0x7C`
> - برای ۲۴۹ بنویسید: `0xF9`

### قطعه کدهای آماده راه‌اندازی تایمر:

#### ۱. راه‌اندازی Timer2 در ATmega8 (فرکانس 8MHz و تیک 1ms):
```c
ASSR  = 0x00;
TCCR2 = (1 << WGM21) | (1 << CS22);  // CTC Mode, Prescaler /64
TCNT2 = 0x00;
OCR2  = 0x7C;                        // 124 دسیمال
TIMSK |= (1 << OCIE2);               // فعال‌سازی وقفه Compare Match
```

#### ۲. راه‌اندازی Timer1 در ATmega8/16/32 (فرکانس 8MHz یا 16MHz با تایمر ۱۶ بیتی):
```c
TCCR1A = 0x00;
TCCR1B = (1 << WGM12) | (1 << CS11) | (1 << CS10); // CTC Mode (Mode 4), Prescaler /64
TCNT1H = 0x00; TCNT1L = 0x00;
// برای 8MHz مقدار OCR برابر 0x7C و برای 16MHz برابر 0xF9 است:
OCR1AH = 0x00; OCR1AL = 0x7C;
TIMSK |= (1 << OCIE1A);              // فعال‌سازی وقفه Timer1 Compare A
```

#### ۳. راه‌اندازی Timer0 در ATmega16/32 (فرکانس 8MHz و تیک 1ms):
```c
TCCR0 = (1 << WGM01) | (1 << CS01) | (1 << CS00);  // CTC Mode, Prescaler /64
TCNT0 = 0x00;
OCR0  = 0x7C;
TIMSK |= (1 << OCIE0);                             // فعال‌سازی وقفه Timer0 Compare
```

---

## 🧩 ۳ شیوه‌ی استفاده (Callback ،Polling و Deferred)

### ۱. حالت بازخوانی سریع در وقفه (ISR Callback)
زمانی که کار انتهای تاخیر بسیار کوتاه است (تغییر یک فلگ یا تغییر وضعیت یک پایه):

```c
void blink_cb(unsigned char slot_id)
{
    PORTB.0 ^= 1; // فقط یک کار فوری چند میکروثانیه‌ای
}

// در main:
async_delay_start_periodic(500, blink_cb); // تکرار هر ۵۰۰ میلی‌ثانیه
```

### ۲. حالت پرس‌وجو دستی (Polling Mode)
زمانی که کار انتهای تاخیر سنگین است (مانند نوشتن روی LCD یا خواندن سنسور):

```c
unsigned char id;

// ارسال (void *)0 به جای callback
id = async_delay_start(200, (void *)0);

while (1)
{
    if (async_delay_elapsed(id)) // آیا زمان سپری شد؟
    {
        update_lcd(); // کارهای سنگین بدون مشکل اجرا می‌شوند
        id = async_delay_start(200, (void *)0); // شروع مجدد
    }
}
```

> ⚠️ **هشدار نشت اسلات:** یک اسلات Polling تا زمانی که تابع `async_delay_elapsed()` برای آن فراخوانی نشود یا با `async_delay_cancel()` لغو نشود، اشغال می‌ماند. اگر بررسی آن را رها کنید، اسلات آزاد نمی‌شود و بعد از پر شدن ظرفیت، توابع `start` مقدار `0xFF` برمی‌گردانند.

### ۳. حالت معوق در حلقه‌ی اصلی (Deferred Callbacks)
اگر ساختار تابع Callback را دوست دارید اما کارهای سنگین (مثل UART یا LCD) دارید:

```c
#define ASYNC_DELAY_DEFERRED_CALLBACKS 1
#include <async_delay.h>

void uart_task_cb(unsigned char slot_id)
{
    puts("Time is up!\r\n"); // در این حالت کاملاً ایمن است!
}

void main(void)
{
    // ... راه‌اندازی تایمر و کتابخانه ...
    async_delay_start_periodic(1000, uart_task_cb);
    #asm("sei")

    while (1)
    {
        // در این حالت صدا زدن poll در حلقه الزامی است!
        async_delay_poll();
    }
}
```

---

## 📋 معرفی توابع کتابخانه

| نام تابع | ورودی و خروجی | کاربرد |
|---|---|---|
| `async_delay_init()` | `void` | ریست شمارنده‌ها و آزادکردن اسلات‌ها (یک‌بار قبل از `sei`). |
| `async_delay_start(dur, cb)` | خروجی `unsigned char` | شروع تاخیر یک‌باره. خروجی: شناسه اسلات یا `0xFF` در صورت پر بودن. |
| `async_delay_start_periodic(dur, cb)` | خروجی `unsigned char` | تاخیر متناوب و تکرارشونده خودکار. |
| `async_delay_elapsed(id)` | ورودی `id`، خروجی `0` یا `1` | بررسی پایان تاخیر در حالت Polling و آزادسازی اسلات. |
| `async_delay_restart(id, dur)` | ورودی `id, dur`، خروجی `0` یا `1` | تمدید/شروع مجدد تاخیر درجا با مدت‌زمان جدید بدون تغییر یا آزادسازی شناسه اسلات. |
| `async_delay_cancel(id)` | ورودی `id` | لغو تاخیر فعال و آزادسازی فوری اسلات آن (دارای الگوریتم لغو سریع $O(1)$). |
| `async_delay_tick()` | درون ISR تایمر | قلب تپنده‌ی کتابخانه. باید فقط در روتین وقفه تایمر فراخوانی شود. |
| `async_delay_poll()` | در حلقه‌ی اصلی | اجرای Callbackهای معوق (فقط زمانی که `DEFERRED_CALLBACKS=1` باشد). |

---

## 💡 قوانین و نکات کلیدی برای برنامه‌نویسان

1. **تمدید و بازنشانی زمان‌سنج‌ها با `async_delay_restart` (الگوی Watchdog و Debounce):**
   زمانی که می‌خواهید زمان یک تایمر را تمدید کنید (مثلاً واچ‌داگ ارتباطات یا حذف جهش کلید)، نیازی به `cancel` و `start` مجدد نیست:
   ```c
   // بازنشانی تاخیر به مدت ۱۰۰۰ میلی‌ثانیه بدون تغییر شناسه و بدون جستجوی اسلات جدید:
   async_delay_restart(wdt_id, 1000);
   ```
2. **لغو سریع اسلات‌ها ($O(1)$ Fast-Path Cancel):**
   در نسخه ۸ با فلگ `ASYNC_DELAY_OPT_FAST_CANCEL`، لغو تایمرهایی که نزدیک‌ترین زمان انقضا را ندارند تنها در ۲۲ سیکل انجام شده و حلقه بررسی کل اسلات‌ها فراخوانی نمی‌شود.
3. **پشتیبانی تا ۱۶ اسلات با نوع بهینه `async_mask_t`:**
   تا ۸ اسلات از ماسک‌های فوق‌سریع ۸ بیتی استفاده می‌شود و بین ۹ تا ۱۶ اسلات، ماسک‌ها به طور خودکار به ۱۶ بیتی تغییر می‌یابند بدون نیاز به تغییر در کدهای کاربر.
4. **مدیریت پروژه‌های چندفایلی (Modular Architecture):** تمام توابع و متغیرهای کتابخانه `static` هستند. هدر `async_delay.h` را **فقط در یک فایل c.** (مثلاً `timer_service.c` یا `main.c`) اینکلود کنید. سایر فایل‌ها باید با متغیرهای `extern` یا توابع رابط با آن ارتباط برقرار کنند.
   ```c
   // در timer_service.c:
   #include <mega8.h>
   #include "timer_service.h"
   #define ASYNC_DELAY_TICK_HZ 1000
   #include <async_delay.h>
   // پیاده‌سازی تایمرها و ISR...
   ```
5. **شناسه‌های قابل بازیافت (Recycled IDs):** شناسه‌های اسلات بعد از پایان تاخیر یا لغو شدن، دوباره به نوبت به تاخیرهای بعدی اختصاص می‌یابند. هرگز شناسه‌ی یک تاخیر پایان‌یافته را دوباره به توابع ندهید.
6. **الگوی ماشین وضعیت خود-زمان‌بند (FSM Self-Rescheduling):** در تاخیرهای یک‌باره، اسلات قبل از اجرای Callback آزاد می‌شود؛ بنابراین می‌توانید گام بعدی ماشین وضعیت را از درون خود Callback زمان‌بندی کنید:
   ```c
   static unsigned char g_step = 0;
   void fsm_cb(unsigned char slot_id)
   {
       g_step++;
       if (g_step == 1) {
           PORTB.0 = 1;
           async_delay_start(100, fsm_cb); // ۱۰۰ میلی‌ثانیه روشن
       } else if (g_step == 2) {
           PORTB.0 = 0;
           async_delay_start(500, fsm_cb); // ۵۰۰ میلی‌ثانیه خاموش
       }
   }
   ```
7. **بررسی صحت نسخه کتابخانه در زمان کامپایل:** برای اطمینان از یکسان بودن هدر کتابخانه در پروژه‌های مختلف، این اعتبارسنجی را در هدرهای برنامه قرار دهید:
   ```c
   #if ASYNC_DELAY_VERSION != 8
   #error "نسخه async_delay.h با پروژه هماهنگ نیست! فایل هدر را بروزرسانی کنید."
   #endif
   ```
8. **محدودیت‌های کامپایلر CodeVisionAVR:**
   - متغیرها باید حتماً در ابتدای بلاک کد تعریف شوند (استاندارد C89).
   - کلمه `bit` یک کلیدواژه رزرو شده در این کامپایلر است؛ متغیری با این نام نسازید (از `slot_bit` استفاده کنید).
   - وقفه‌ها را با `#asm("sei")` روشن و با `#asm("cli")` خاموش کنید.
   - از پسوندهای عددی نظیر `U` خودداری فرمایید (مثلاً `1000` به جای `1000U`).

---

## 🔍 جدول رفع اشکال سریع

| مشکل | علت احتمالی | راه‌حل |
|---|---|---|
| زمان‌ها نمی‌گذرند یا `elapsed` همیشه 0 است | وقفه تایمر فعال نیست یا `sei` فراموش شده | بررسی رجیستر `TIMSK` و فراخوانی `#asm("sei")`. |
| زمان تاخیرها نصف یا دو برابر شده است | کلاک پروژه در تنظیمات CodeVision با کلاک واقعی یکی نیست یا OCR اشتباه است | محاسبه مجدد OCR و هماهنگ‌کردن کلاک پروژه. |
| تابع `start` همیشه `0xFF` برمی‌گرداند | نشت اسلات در حالت Polling یا پر شدن ظرفیت | اطمینان از صدا زدن `async_delay_elapsed` برای تمام تاخیرها یا افزایش `MAX_SLOTS`. |
| در حالت Deferred کالبک‌ها اجرا نمی‌شوند | تابع `poll` فراخوانی نشده است | افزودن `async_delay_poll()` درون `while(1)`. |
| خطای `undefined symbol 'SREG'` | تقدم اینکلود رعایت نشده | قرار دادن هدر میکرو (مانند `mega8.h`) قبل از `async_delay.h`. |
| خطای `invalid combination of type specifiers` | استفاده از نام `bit` برای متغیر | تغییر نام متغیر به عنوان مثال `slot_bit`. |
